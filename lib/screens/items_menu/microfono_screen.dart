import 'package:flutter/material.dart';

class MicrofonoScreen extends StatelessWidget {
  const MicrofonoScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Dictado', style: TextStyle(fontSize: 24)),
    );
  }
} 